// Wrap both SigninForm and SignupForm components, so the styling is consistent between both forms
// Example: both forms will have the same logo and image in the same placement
import React from 'react'

const AuthLayout = () => {
  return (
    <div>AuthLayout</div>
  )
}

export default AuthLayout